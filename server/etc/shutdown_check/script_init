# Begin of generic script initialisation
SCRIPT_NAME="`basename $0`"

# Load default configuration
[ -f "/etc/shutdown_check/$SCRIPT_NAME.conf" ] && . /etc/shutdown_check/$SCRIPT_NAME.conf

# End of generic script initialisation
